---
import Layout from '../layouts/Layout.astro'
import { RootAppEntry } from '../RootAppEntry.tsx'
import i18next from 'i18next'
---

<Layout title={`${i18next.t('pages.index.title')} - FileUni`}>
	<RootAppEntry client:only="react" />
</Layout>
